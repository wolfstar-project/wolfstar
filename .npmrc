# Intentionally empty.
#
# Since pnpm 11, only authentication and registry settings are read from .npmrc.
# Every other setting must be declared in pnpm-workspace.yaml using camelCase keys.
# The former contents (shamefully-hoist, ignore-workspace-root-check, hoist-pattern)
# were silently inert here and now live in pnpm-workspace.yaml.
